<UserControl x:Class="ScreenTimeTracker.Views.Controls.SidebarNav"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontFamily" Value="{StaticResource SFPro}" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="NavBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="12,8"
                                Margin="8,2">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="NavBorder" Property="Background"
                                        Value="{StaticResource SidebarHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel Margin="0,16,0,0">
            <!-- App Logo -->
            <TextBlock Text="⏱" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,4" />
            <TextBlock Text="屏幕时间"
                       Style="{StaticResource Title3}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20" />

            <!-- 仪表盘 -->
            <Button Command="{Binding NavigateToDashboardCommand}"
                    Style="{StaticResource NavButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📊" Width="20" VerticalAlignment="Center" Margin="0,0,10,0" />
                    <TextBlock Text="仪表盘" VerticalAlignment="Center" />
                </StackPanel>
            </Button>

            <!-- 设置 -->
            <Button Command="{Binding NavigateToSettingsCommand}"
                    Style="{StaticResource NavButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⚙️" Width="20" VerticalAlignment="Center" Margin="0,0,10,0" />
                    <TextBlock Text="设置" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
